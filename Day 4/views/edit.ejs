<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            color: #4c90af;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 60%;
            max-width: 400px;
            margin-bottom: 20px;
        }

        form input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        form button {
            padding: 10px 15px;
            background-color: #4c90af;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
            width: 100%;
        }

        form button:hover {
            background-color: #3b6e87;
        }
    </style>
</head>
<body>
    <h1>Edit Details</h1>
    <form method="post" action="/update">
        <input type="text" name="id" value="<%=singleData.id%>" placeholder="Enter Student Name" hidden/>
        <input type="text" name="name" value="<%=singleData.name%>" placeholder="Enter Student Name" required/>
        <input type="text" name="course" value="<%=singleData.course%>" placeholder="Enter Student Course" required/>
        <button type="submit">Update</button>
    </form>
</body>
</html>
